# number of iterations
iterations = 5

# the file containing the words you would like to learn - must be located in word_lists directory
# the file must specify the foreign language on the first line
reference_file = "german2000.txt"

# whether mistakes should be recorded
record_mistakes = true

# whether audio mode should be available - only macOS with French supported at moment
audio_mode = false

# save the results to your results file
save_results = true

# give question in english, rather than the foreign language
give_question_in_english = false

# file to append the results to when the flashcards are complete
save_results_file = "reports/results2.csv"

# file to add record of user answers to questions
answer_history_file = "history/mistakes2.json"